import { Box } from "@mui/material";

export default function SidebarNav() {
    return (
        <Box width={`250px`} minHeight={`100%`} borderRight={`3px solid darkgray`} bgcolor={`#0b2431`} color={`white`}>
            
        </Box>
    )
}